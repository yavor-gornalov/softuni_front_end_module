// https://judge.softuni.org/Contests/Practice/Index/4362#3

function solve(text, index, count) {
    let result = text.substring(index, index + count)
    console.log(result);
}

solve("ASentence", 1, 8);
solve("SkipWord", 4, 7);
